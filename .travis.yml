language: php
sudo: false

php:
  - '7.0'

before_script:
  - composer install

script:
  - phpunit --log-junit email.xml --bootstrap src/Email.php tests/EmailTest.php

after_script:
  - python <(curl -s https://raw.githubusercontent.com/report-ci/scripts/master/upload.py)